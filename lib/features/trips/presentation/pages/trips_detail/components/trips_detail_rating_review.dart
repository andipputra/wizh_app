import 'package:flutter/material.dart';

class TripsDetailRatingReview extends StatelessWidget {
  const TripsDetailRatingReview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}